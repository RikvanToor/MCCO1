begin
  y := 2;
  z := 1;

  q := y + z;
  h := 2 + y;

  if h < q + 1 then {
    deze_is_moeilijk := q + z + 2 * h) / (y - z);
  } else {
    nog_eens := y * y * y * y;
    skip;
    skip;
    skip;
  }
end
